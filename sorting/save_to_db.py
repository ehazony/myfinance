# from myFinance.models import Transaction, Tag
#
#
# def save_transaction(date, name, price, tag_name):
#     tag_obj = Tag.objects.get(name=tag_name)
#     Transaction.objects.create(date=date, name=name, value=price, tag=tag_name, tag_ref=tag_obj)
#
#
